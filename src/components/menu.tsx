import * as React from 'react';
import { Form } from './building-blocks';

interface MenuProps{

}
interface MenuState{}
export class Menu extends React.Component<MenuProps, MenuState> {
    render() {
        return(
            <div />
        );
    };
}